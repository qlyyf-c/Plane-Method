# Railway 配置文件

[build]
# 使用安装脚本
command = "cp docker/Dockerfile.railway Dockerfile && ./deploy_to_railway.sh"

[start]
# 使用启动脚本
command = "./start.sh"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30